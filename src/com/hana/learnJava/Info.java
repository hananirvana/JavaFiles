package com.hana.learnJava;

public interface Info {
	public void showInfo();

	public void changeInfo();

	// This is an interface. I can't put any code in this interface.
	// If you look at the code above, the methods above doesn't need a curly bracket
	// as we can't put code.
}
